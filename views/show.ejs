<%- include('partials/header'); -%>
    <div class="container">
        <div class="row row4 align-items-center my-3">
            <div class="col-10">
                <div class="card bg-dark my-3 text-white">
                    <div class="card-header">
                        <h1><%=note.title %> <a href="/diary/mynotes" class="btn-close m-3 py-1 px-3 bg-light text-end" aria-label="Close"></a></h1>
                        
                    </div>

                    <div class="card-body">
                        <div class="card-text">
                            <%=note.textnote %> 
                        </div>
                    </div>
                    <div class="card-footer text-muted">
                         Last updated <%=note.mytime %> 
                    </div>
                </div>
                <a class="btn btn-warning" href="/diary/<%=note._id %>/edit ">Edit me</a>
                <form class="d-inline" action="/diary/<%=note._id%>?_method=DELETE" method="POST">
                    <button class="btn btn-danger">DELETE</button>
                </form>
                <!-- <h1>
                    <%=note.title%>
                </h1>
                <h6>
                    <%=note.textnote %>
                </h6>  -->
            </div>

        </div>
        
    </div>
    <%- include('partials/footer'); -%>
        </body>

        </html>